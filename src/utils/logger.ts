// import Bugsnag from "@bugsnag/expo";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const log = (_error: string | unknown) => {
  //   Bugsnag.notify(error);
};

const start = () => {
  //   Bugsnag.start();
};

export default { log, start };
